# Chromium / CEF runtime caches
DawnCache/
GPUCache/
debug.log

# Build & release outputs
dist/
publish/
*.zip

# 本地曲库审计产物（含整份曲库清单：歌名/歌手/服务端路径 ⇒ 绝不可入库）
.audit/

# 误建的空占位文件
/app.js

# Android 构建中间产物（编译出的 class / dex / APK / 中间截图；由 build.ps1 重新生成）
subsonic-player/android/build/

# 会话临时目录（探针脚本、截图、日志导出）
build-tmp/
